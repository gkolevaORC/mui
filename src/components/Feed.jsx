import { Box, Paper, Table, TableBody, TableCell, TableContainer, TableHead, TableRow } from '@mui/material'
import React, { useEffect, useState } from 'react'

const Feed = () => {

  return (
    <Box bgcolor="pink" flex={1} p={5} sx={{display: { xs:"none", sm:"block" }}}>
    </Box>
  );
}

export default Feed